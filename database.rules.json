{
  "rules": {
    ".read": true,
    ".write": false,
    "orders": {
      "$uid": {
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "users": {
      "$uid": {
        ".write": "auth != null && auth.uid === $uid"
      }
    }
  }
}
